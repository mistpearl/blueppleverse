export type Prototype = {
    id: string;
    name: string;
    description: string;
    release: string;
    playerCount: string;
    gameTime: string;
    categoryId: string;
    authorId: string;

};

export type Category = {
    id: string;
    name: string;
    description: string;
};
